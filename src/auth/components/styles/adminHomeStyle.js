import { makeStyles } from "@material-ui/core";

 const adminHomeStyle  = makeStyles((theme)=>({
    container:{
        display:'flex',
        flexDirection:'column',
        
    }
 }))

 export default adminHomeStyle